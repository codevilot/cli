name: Test

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  bash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install ShellCheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: Bash syntax check
        run: |
          bash -n entry.sh
          bash -n commands/*.sh
          bash -n lib/*.sh
          bash -n tests/*.sh
      - name: ShellCheck
        run: shellcheck -S warning entry.sh commands/*.sh lib/*.sh tests/*.sh
      - name: Tests
        run: ./tests/run-tests.sh
